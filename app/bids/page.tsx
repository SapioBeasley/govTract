import { FileText } from "lucide-react";
import { SectionShell } from "@/components/section-shell";

export default function BidsPage() {
  return <SectionShell eyebrow="Respond" title="Bids" description="Build solicitation-specific response workspaces with requirements, drafting, and review in one place." icon={FileText} />;
}
