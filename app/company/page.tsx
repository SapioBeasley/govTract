import { Building2 } from "lucide-react";
import { SectionShell } from "@/components/section-shell";

export default function CompanyPage() {
  return <SectionShell eyebrow="Profile" title="Company" description="Maintain the capabilities, qualifications, geography, and past performance used for matching and bid drafting." icon={Building2} />;
}
