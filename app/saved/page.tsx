import { Heart } from "lucide-react";
import { SectionShell } from "@/components/section-shell";

export default function SavedPage() {
  return <SectionShell eyebrow="Pipeline" title="Saved" description="Keep promising opportunities organized as they move from review to pursuit." icon={Heart} />;
}
